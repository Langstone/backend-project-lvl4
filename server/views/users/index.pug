extends ../layouts/application.pug

block content
  table.table.table-striped
    thead
      tr
        th= t('views.users.id')
        th= t('views.users.fullName')
        th= t('views.users.email')
        th= t('views.users.createdAt')
        th= t('views.users.actions')
    tbody
    each user in users
      tr
        td= user.id
        td= `${user.firstName} ${user.lastName}`
        td= user.email
        td= formatDate(user.createdAt)
        td 
          <div class="btn-group">
            +linkTo(route('editUser', { id: user.id }))(class='btn btn-primary' value=t('views.users.change'))
            +buttonTo(route('deleteUser', { id: user.id }), 'delete')(class='btn btn-danger' value=t('views.users.delete'))
          </div>
